public class _View extends JPanel {
    private _ViewModel viewModel;
    private _Controller controller;

    public _View(_ViewModel viewModel, _Controller controller) {
        this.viewModel = viewModel;
        this.controller = controller;
    }
}
